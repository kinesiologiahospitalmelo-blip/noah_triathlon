{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd noah-coach && npm install && npm run build",
  "outputDirectory": "noah-coach/build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/app.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
